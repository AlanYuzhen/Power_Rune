# RP-26Rune 在 Ubuntu 24.04 上的适配说明

> **适配：齐鲁工业大学 Adam 战队　于震（3244378316@qq.com）**

> 本文档记录把 RP-26Rune（原适配环境 Ubuntu 22.04）移植到 **Ubuntu 24.04 (x86-64)** 时遇到的全部问题、修复方法、改动清单与验证结果。
> 适配日期：2026-09-18。适配完成后项目可完整构建并运行（OpenVINO 推理 + Foxglove 可视化 + 能量机关检测全链路通过）。
>
> **原则：业务/算法代码一行未改，所有改动都在第三方依赖的版本对齐与构建配置上。**

---

## 1. 环境信息

| 项目 | 版本 / 说明 |
| --- | --- |
| 操作系统 | Ubuntu 24.04 LTS，x86-64 (amd64) |
| 编译器 / CMake | gcc/g++（系统自带）、CMake 3.28 |
| OpenCV | 4.6.0（apt） |
| OpenVINO | 2024.4.0 |
| Eigen | 3.4.0 |
| Sophus | 1.22.10（源码安装） |
| Ceres | 2.3.0（静态库 `/usr/local/lib/libceres.a`，master 源码构建） |
| Boost | 1.83（apt） |
| glog / gflags | apt |
| flatbuffers（apt） | 2.0.8（**版本过低，项目不用它**，见问题 2） |
| abseil-cpp（absl） | **20260107.0，共享库版（含两处 ABI 补丁）**，见问题 4、5 |
| foxglove-sdk | **v0.26.0** 预编译 C++ SDK，见问题 1、6 |

---

## 2. 问题与修复（按处理顺序）

### 问题 1：foxglove 静态库 `libfoxglove.a` 缺失

- **现象**：链接 `foxglove_lib` 时报找不到 `src/core/utility/foxglove/lib/libfoxglove.a`。
- **原因**：该预编译库被 `.gitignore`（`*.a`）排除，仓库 clone 下来后不存在；仓库内只有 foxglove-sdk 的 C++ 包装层源码。
- **修复**：从 foxglove-sdk 官方 release 下载 **v0.26.0** 的 C++ 预编译包（与仓库内头文件逐版本比对 v0.25.3 / v0.26.0 / v0.27.0 后确定），把其中的 `lib/libfoxglove.a` 复制到：
  `src/core/utility/foxglove/lib/libfoxglove.a`
- 下载地址：
  `https://github.com/foxglove/foxglove-sdk/releases/download/sdk%2Fv0.26.0/foxglove-v0.26.0-cpp-x86_64-unknown-linux-gnu.zip`

### 问题 2：flatbuffers 版本不匹配（编译期 static_assert 失败）

- **现象**：编译 foxglove 自动生成代码（`autogenerated_flatbuffers/*_generated.h`）时静态断言失败，要求
  `FLATBUFFERS_VERSION_MAJOR==25 && MINOR==2 && REVISION==10`（即 **v25.2.10**），而 apt 的 `libflatbuffers-dev` 是 2.0.8。
- **修复**：
  1. 下载 flatbuffers v25.2.10，把**头文件**放到仓库
     `src/core/utility/foxglove/third_party/flatbuffers/include/flatbuffers/`（共 32 个头文件）；
  2. 修改 `src/core/utility/foxglove/CMakeLists.txt`，给 `foxglove_lib` 与 `foxglove_interface_lib`
     的 include 路径加入该 third_party 目录（见第 3 节 diff）。
- apt 的 flatbuffers 2.0.8 **无需卸载**，项目不再引用它。
- 下载地址：`https://github.com/google/flatbuffers/archive/refs/tags/v25.2.10.tar.gz`

### 问题 3：cloud_sink 符号未定义（死代码）

- **现象**：运行时 `libfoxglove_lib.so: undefined symbol: foxglove_cloud_sink_stop / foxglove_cloud_sink_start`。
- **原因**：cloud_sink 是项目作者自定义新增的 C API，**foxglove 官方所有发布版（v0.25.3/0.26.0/0.27.0）都不含该 API**；
  且全项目检索确认 CloudSink **没有任何调用点，属于死代码**。
- **修复**：新增桩实现 `src/core/utility/foxglove/src/cloud_sink_stub.cpp`，
  按 `foxglove-c.h` 中的签名补出 `foxglove_cloud_sink_start`（返回 `FOXGLOVE_ERROR_UNSPECIFIED`）
  与 `foxglove_cloud_sink_stop`（返回 `FOXGLOVE_ERROR_OK`）。该目录用 `file(GLOB ...)` 自动收集源码，无需改 CMake。

### 问题 4（核心）：absl 与 Ceres 2.3.0 静态库 ABI 不匹配（67 个未定义符号）

- **现象**：`ldd -r output/libpower_rune_lib.so` 报 60 余个未解析的 `absl::` 符号
  （`log_internal::LogMessage` 系列、`container_internal` 哈希表系列等）。
- **定位过程**：
  1. `nm` / `ldd -r` 导出全部缺失符号并分类，确认均为 absl 符号；
  2. 用 `nm -g` 查看 `/usr/local/lib/libceres.a` 引用的精确修饰名（mangled name）；
  3. 逐版本下载 absl 源码 grep 比对，确认 libceres.a 是对 **absl 20260107** 时代的源码编译的
     （需要 SOO 哈希表 `GrowSooTableToNextCapacityAndPrepareInsert`、`GetRefForEmptyClass`、
     `PrepareInsertLarge` 等旧版 20240116 完全没有的符号）。
- **直接安装 absl 20260107 仍链接不上**，有两处 ABI 细节必须打补丁后重新编译 absl：
  1. **关闭 inline namespace**：LTS 版默认开启，符号名带 `absl::lts_20260107::...` 前缀，
     而 libceres.a 引用的是无前缀的 `absl::...`。
     修改 `absl/base/options.h`：
     `#define ABSL_OPTION_USE_INLINE_NAMESPACE 1` → `0`。
  2. **位掩码类型改回 uint32_t**：新版 SSE2 分组用 `NonIterableBitMask<uint16_t, 16>`，
     而 libceres.a 引用的是 `NonIterableBitMask<uint32_t, 16, 0>`（修饰名里 `Ij` vs `It`）。
     修改 `absl/container/internal/hashtable_control_bytes.h` 中 `GroupSse2Impl`：
     `using NonIterableBitMaskType = NonIterableBitMask<uint16_t, kWidth>;`
     → `NonIterableBitMask<uint32_t, kWidth>;`。
- **构建命令**（静态库阶段，后被问题 5 的共享库方案取代）：
  ```bash
  cmake -S abseil-cpp-20260107.0 -B absl-build \
    -DCMAKE_BUILD_TYPE=Release -DCMAKE_POSITION_INDEPENDENT_CODE=ON \
    -DABSL_BUILD_TESTING=OFF -DABSL_USE_EXTERNAL_GOOGLETEST=OFF -DABSL_ENABLE_INSTALL=ON
  cmake --build absl-build --parallel 2
  sudo cmake --install absl-build
  ```
- 下载地址：`https://github.com/abseil/abseil-cpp/archive/refs/tags/20260107.0.tar.gz`

### 问题 5：absl flags 重复注册导致运行时 abort（静态库 → 共享库）

- **现象**：问题 4 解决后程序能加载模型，但随即崩溃：
  `ERROR: Something is wrong with flag 'stderrthreshold' in file '.../absl/log/flags.cc' ...
  being linked both statically and dynamically into this executable`。
- **原因**：项目有 14 个插件式 `.so`，静态链接时每个 `.so` 各自打包一份 absl，
  运行时 dlopen 后同一个 gflag（如 `stderrthreshold`）被注册多次，absl 直接 abort。
- **修复**：把 absl 改为**共享库**构建并安装到 `/usr/local`（91 个 `libabsl_*.so`），
  所有插件动态链接同一份 absl，flags 注册表全局唯一。补丁（问题 4 的两处）保持不变：
  ```bash
  cmake -S abseil-cpp-20260107.0 -B absl-shared \
    -DCMAKE_BUILD_TYPE=Release -DBUILD_SHARED_LIBS=ON \
    -DABSL_BUILD_TESTING=OFF -DABSL_USE_EXTERNAL_GOOGLETEST=OFF -DABSL_ENABLE_INSTALL=ON
  cmake --build absl-shared --parallel 2
  sudo cmake --install absl-shared
  ```
- 安装后 CMake 的 `absl::*` targets 自动指向 `.so`，项目重新 configure + 构建即可，无需改项目 CMake。
- 另需把 `/usr/local/lib/cmake/Ceres/CeresConfig.cmake` 中
  `set(_CERES_ABSL_VERSION ...)` 改为 **20260107**（absl 的版本配置采用 ExactVersion 策略，必须精确一致）。

### 问题 6：foxglove C 接口头文件与预编译库结构体布局不匹配（运行时参数处理器报错）

- **现象**：Foxglove WebSocket 服务创建时崩溃：
  `Value or argument is invalid: foxglove_parameter_handler requires both 'get' and 'set' to be non-NULL`。
- **原因**：仓库自带的 `foxglove-c/foxglove-c.h` 是**旧版**，其中 `foxglove_server_options` 结构体
  比 v0.26.0 库少了 `session_id`、`message_backlog_size`、`parameter_handler` 三个尾部字段。
  包装层用 `= {}` 零初始化的是旧结构体内存，v0.26.0 的库按新布局读取时，
  `parameter_handler` 字段落在结构体之外、读到栈上的非零垃圾值，于是误判"只设置了 get/set 其中一个"。
- **修复**：
  1. 用 v0.26.0 官方 `foxglove-c.h` 替换仓库内的同名文件（原文件备份为 `foxglove-c.h.bak_2409`）；
  2. 官方头不含作者自定义的 cloud_sink 声明，把备份头中的 4 段 cloud_sink 声明
     （前置 typedef、`foxglove_cloud_sink_callbacks`、`foxglove_cloud_sink_options`、
     `foxglove_cloud_sink_start/stop`）按原 `#if !defined(__wasm__)` 守卫合并回新头对应位置。

---

## 3. 仓库内改动文件清单

`cd ~/Alan/RP-26Rune && git status` 可见：

| 状态 | 文件 | 说明 |
| --- | --- | --- |
| 修改 | `scripts/setup_ubuntu22.sh` | 系统版本检查由仅允许 22.04 改为同时允许 22.04 / 24.04 |
| 修改 | `src/core/utility/foxglove/CMakeLists.txt` | foxglove 两个 target 的 include 路径加入 `third_party/flatbuffers/include` |
| 修改 | `src/core/utility/foxglove/include/foxglove-c/foxglove-c.h` | 升级为 v0.26.0 官方头并合并 cloud_sink 声明 |
| 新增 | `src/core/utility/foxglove/include/foxglove-c/foxglove-c.h.bak_2409` | 原旧版头文件备份 |
| 新增 | `src/core/utility/foxglove/lib/libfoxglove.a` | foxglove-sdk v0.26.0 预编译静态库（约 59 MB） |
| 新增 | `src/core/utility/foxglove/third_party/flatbuffers/include/flatbuffers/` | flatbuffers v25.2.10 头文件（32 个） |
| 新增 | `src/core/utility/foxglove/src/cloud_sink_stub.cpp` | cloud_sink 桩实现 |

`foxglove/CMakeLists.txt` 的关键改动：

```cmake
target_include_directories(${lib_name}_lib PUBLIC
    ${foxglove_SOURCE_DIR}/include
    ${foxglove_SOURCE_DIR}/third_party/flatbuffers/include
)
# interface 库同理，追加 third_party/flatbuffers/include
```

`setup_ubuntu22.sh` 的关键改动：

```bash
# 原：仅允许 22.04
[[ "${ID:-}" == "ubuntu" && ("${VERSION_ID:-}" == "22.04" || "${VERSION_ID:-}" == "24.04") ]] ||
    die "this script supports Ubuntu 22.04 and 24.04 only (detected: ${PRETTY_NAME:-unknown})"
```

## 4. 系统目录（/usr/local）改动

| 路径 | 改动 |
| --- | --- |
| `/usr/local/lib/libabsl_*.so`（91 个）及 `/usr/local/include/absl/`、`/usr/local/lib/cmake/absl/` | 安装 absl 20260107 **共享库版**（含 inline namespace 关闭、uint32_t 掩码两处补丁）；旧的 20240116 静态库被同名覆盖 |
| `/usr/local/lib/cmake/Ceres/CeresConfig.cmake` | `set(_CERES_ABSL_VERSION 20260107)`（原为 20240116） |
| `/usr/local/lib/libceres.a`、Ceres 头文件 | **未改动**（保持原 2.3.0 静态库） |

> apt 安装的系统包（`/usr/lib/x86_64-linux-gnu/` 下的 absl 20220623、flatbuffers 2.0.8 等）均未改动；
> CMake 通过 `/usr/local` 优先找到新版本。

## 5. 构建与运行

```bash
cd ~/Alan/RP-26Rune

# 配置 + 构建
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --parallel 2

# 运行（无显示器 / 远程）
./run_demo.sh --headless

# 运行（接显示器，可看到 Qt 可视化窗口）
./run_demo.sh
```

- `run_demo.sh` 会自动重新 configure、构建并启动 `output/app`；
- headless 模式自动设置 `QT_QPA_PLATFORM=offscreen`；
- 演示使用内置仿真 ECS（BigRune / Red）与回放视频
  `src/app_plugin/single_camera_manager/config/video/big_rune_demo.mp4`，无需真实硬件。

## 6. 验证结果（2026-09-18）

1. **完整构建通过**：`cmake --build build` 全部 target 成功，产物在 `output/`（`app` + 14 个 `.so`）。
2. **符号完整性**：对全部 14 个 `.so` 执行 `ldd -r`，未定义符号数全部为 **0**：
   ```bash
   for f in output/*.so; do echo "$f: $(ldd -r "$f" 2>&1 | grep -c 'undefined symbol')"; done
   ```
3. **运行链路通过**（`./run_demo.sh --headless` 日志）：
   - `[NNDetector] initialized OpenVINO rune model ... model-0624.onnx, device=CPU`（模型加载成功）；
   - `context initialized; plugin thread groups are running`；
   - `[ReceiveDecoder] simulated ECS enabled: mode=BigRune, color=Red`；
   - `[CameraManager] replay opened: ... big_rune_demo.mp4, fps=30, loop=1`；
   - `[TrackerManager] 26-style TFTree initialized`；
   - Foxglove WebSocket server 创建成功（无参数处理器报错）；
   - `[BigRunePhaseMotionFilter] 顺时针`（能量机关相位运动检测正常输出），进程持续稳定运行。
   - 日志中 `This plugin does not support propagateSizeHints()` 为 Qt offscreen 平台的无害提示。

## 7. 重新部署 / 重装系统时的注意事项

若在另一台 Ubuntu 24.04 机器上重新部署，除拉取本仓库（含上述新增文件）外，还需在系统层重装一次打补丁的 absl：

1. 下载并解压 `abseil-cpp-20260107.0`；
2. 应用两处补丁：
   - `absl/base/options.h`：`ABSL_OPTION_USE_INLINE_NAMESPACE` 改为 `0`；
   - `absl/container/internal/hashtable_control_bytes.h`：`GroupSse2Impl` 的
     `NonIterableBitMaskType` 模板参数由 `uint16_t` 改为 `uint32_t`；
3. 按问题 5 的命令以 `-DBUILD_SHARED_LIBS=ON` 构建并 `sudo cmake --install`；
4. 确认 `/usr/local/lib/cmake/Ceres/CeresConfig.cmake` 中 `_CERES_ABSL_VERSION` 为 `20260107`；
5. 重新构建项目并按第 6 节验证。

> 注意：absl 必须与本机 `/usr/local/lib/libceres.a` 的 ABI 精确匹配（无 inline namespace、uint32_t 掩码）。
> 若将来重新从源码构建 Ceres，则可改用与之配套的任意 absl 版本，但需重新验证全部 `.so` 的未定义符号数为 0。

---

*适配过程中未修改任何能量机关算法业务代码；OpenVINO、OpenCV、Ceres 本体与 apt 系统包均保持原样。*
